{
  "name": "ogretmenevi-root",
  "private": true,
  "scripts": {
    "setup": "npm --prefix api install && npm --prefix admin install",
    "dev": "npx -y npm-run-all setup dev:all",
    "api:dev": "npm run dev --prefix api",
    "admin:dev": "npm run dev --prefix admin || echo admin folder missing",
    "dev:db": "docker compose -f docker-compose.local.yml up -d || echo docker yok, db atlanıyor",
    "dev:api": "npx -y kill-port 4000 && npm --prefix api run dev",
    "dev:admin": "npx -y kill-port 3000 && npm --prefix admin run dev",
    "dev:all": "run-p dev:db dev:api dev:admin",
    "check": "pnpm -r typecheck && pnpm -r lint && pnpm -r test"
  },
  "devDependencies": {
    "kill-port": "^2.0.1",
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {
    "expo": "^53.0.20",
    "expo-asset": "~11.1.7",
    "expo-file-system": "~18.1.11"
  }
}
